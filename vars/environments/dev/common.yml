# Common/shared variables
variables:
  environment: dev
  publishTestArtifacts: true

  # Append suffix to deploymentVersion, allowing multiple deployments, with optional cleanup
  enableDynamicDeploymentSuffix: true
  cleanUpStackAfterTest: true
