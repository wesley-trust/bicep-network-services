# Common/shared variables
variables:
  environment: dev
  publishTestArtifacts: true

  # For test pipelines, append a unique suffix to the deploymentVersion to allow multiple deployments without conflict
  enableDynamicDeploymentSuffix: true