# Common/shared variables
variables:
  environment: dev
  publishTestArtifacts: true

  # Append suffix to deploymentVersion for Tests, to allow multiple runs, with optional cleanup
  enableTestsDeploymentSuffix: true
  cleanUpStackAfterTest: true
